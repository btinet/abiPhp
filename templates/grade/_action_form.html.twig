<div class="BtnGroup-item">
    <a class="BtnGroup-item btn btn-sm color-fg-sponsors" href="{{ path('app_grade_edit', {'id': grade.id}) }}"><i class="bi bi-pencil"></i></a>
    <button form="form" class="BtnGroup-item btn btn-sm color-fg-danger"><i class="bi bi-trash"></i></button>
</div>

<form id="form" method="post" action="{{ path('app_grade_delete', {'id': grade.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ grade.id) }}">
</form>
