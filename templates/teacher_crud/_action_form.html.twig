<div class="BtnGroup-item">
    <a class="BtnGroup-item btn btn-sm color-fg-sponsors" href="{{ path('app_teacher_crud_show', {'id': teacher.id}) }}"><i class="bi bi-search"></i></a>
    <a class="BtnGroup-item btn btn-sm color-fg-sponsors" href="{{ path('app_teacher_crud_edit', {'id': teacher.id}) }}"><i class="bi bi-pencil"></i></a>
    <button form="form" class="BtnGroup-item btn btn-sm color-fg-danger"><i class="bi bi-trash"></i></button>
</div>


<form id="form" method="post" class="d-inline-block" action="{{ path('app_teacher_crud_delete', {'id': teacher.id}) }}" onsubmit="return confirm('Möchten Sie wirklich diesen Eintrag löschen?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ teacher.id) }}">

</form>
