{% extends '_admin.html.twig' %}

{% block title %}Teacher{% endblock %}

{% block body %}
    <h1>Teacher</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ teacher.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ teacher.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ teacher.lastname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_teacher_crud_index') }}">back to list</a>

    <a href="{{ path('app_teacher_crud_edit', {'id': teacher.id}) }}">edit</a>

    {{ include('teacher_crud/_delete_form.html.twig') }}
{% endblock %}
